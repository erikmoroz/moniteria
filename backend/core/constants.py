"""Application-wide constants."""

# Legal document versions — bump these when the content changes.
# When bumped, the registration form will require users to accept the new version,
# and a new consent record will be created (the old one is NOT auto-withdrawn).
TERMS_VERSION = '1.0'
PRIVACY_VERSION = '1.0'
